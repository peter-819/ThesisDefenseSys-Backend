type (
	ExcelPreviewReply {
		Data [][]string `json:"data"`
	}
)
type (
	RegisterBatchReply {
	}
)
@server(
	jwt: Auth
)
service Admin-api{
	@handler excelpreview
	post /admin/excel/preview returns (ExcelPreviewReply)
	
	@handler registerbatch
	post /admin/register/batch returns (RegisterBatchReply)
	
	// @handler registerteachers
	// post /admin/register/teachers returns (RegisterTeachersReply)
}